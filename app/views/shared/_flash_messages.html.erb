<% flash.each do |type, msg| %>
  <div class="alert alert-<%= type == "alert" ? "danger" : "info" %> alert-dismissible fade show mt-1"
    role="alert">
    <%= msg %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
